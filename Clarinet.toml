[project]
name = "hidden-tree-api"
authors = ["Hidden Tree Development Team"]
description = "A decentralized rental marketplace with transparent, secure agreement management"
telemetry = false
requirements = []
[contracts.tree-rental-marketplace]
path = "contracts/tree-rental-marketplace.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
